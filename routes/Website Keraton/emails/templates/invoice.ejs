<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
    <div class="max-w-4xl mx-auto py-16 my-auto bg-white shadow-lg rounded-lg">
        <header class="flex justify-between items-center px-8 py-4 border-b">
            <div class="flex justify-between items-center gap-2">
                <img src="/public/assets/email/logo.svg" height="50px" width="50px">
                <h1 class="text-3xl font-bold text-gray-800">Invoice</h1>
            </div>
            <div class="text-right items-center">
                <p class="text-gray-600">Invoice: <span class="font-semibold">
                        <%= nomor_invoice %>
                    </span></p>
                <p class="text-gray-600">Date: <span class="font-semibold">
                        <%= date %>
                    </span></p>
            </div>
        </header>
        <main class="px-8 py-4">
            <div class="flex w-full justify-between">
                <section class="mb-8 w-3/4">
                    <h2 class="text-xl font-semibold text-gray-800">Bill To:</h2>
                    <p class="text-gray-600">name <span class="font-semibold">
                            <%= name %>
                        </span></p>
                    <p class="text-gray-600">email <span class="font-semibold">
                            <%= email %>
                        </span>
                    </p>
                </section>
                <img src="/public/assets/email/testqr.jpg" height="100px" width="100%" class="px-auto w-1/4">
            </div>
            <section></section>
            <section>
                <table class="min-w-full bg-white">
                    <thead>
                        <tr>
                            <th class="py-2 px-4 border-b text-left text-gray-800">Description</th>
                            <th class="py-2 px-4 border-b text-right text-gray-800">Quantity</th>
                            <th class="py-2 px-4 border-b text-right text-gray-800">Price</th>
                            <th class="py-2 px-4 border-b text-right text-gray-800">Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(var i=0 ; i < invoices.length; i++) { %>
                            <tr>
                                <td class="py-2 px-4 border-b text-gray-600">
                                    <%= invoices[i].item_desc %>
                                </td>
                                <td class="py-2 px-4 border-b text-right text-gray-600">
                                    <%= invoices[i].quantity %>
                                </td>
                                <td class="py-2 px-4 border-b text-right text-gray-600">
                                    <%= invoices[i].price %>
                                </td>
                                <td class="py-2 px-4 border-b text-right text-gray-600">
                                    <%= invoices[i].total %>
                                </td>
                            </tr>
                            <% } %>
                    </tbody>
                </table>
            </section>
        </main>
        <footer class="px-8 py-4 border-t">
            <div class="text-right">
                <p class="text-gray-600">Subtotal: <span class="font-semibold">
                        <%= subtotal %>
                    </span></p>
                <p class="text-gray-600">Tax: <span class="font-semibold">
                        <%= tax %>
                    </span></p>
                <p class="text-gray-600">Total: <span class="font-semibold">
                        <%= total %>
                    </span></p>
            </div>
        </footer>
    </div>
</body>

</html>